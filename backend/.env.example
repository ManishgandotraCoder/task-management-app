# Database Configuration
# You can use either DATABASE_URL or individual variables

# Option 1: Use DATABASE_URL (recommended for production)
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://username:password@localhost:5432/task_management

# Option 2: Use individual variables (recommended for development)
# Uncomment and configure these if not using DATABASE_URL
# DB_HOST=localhost
# DB_PORT=5432
# DB_USERNAME=postgres
# DB_PASSWORD=your_password_here
# DB_NAME=task_management

# Database Options
# Set to true to auto-sync database schema (use only in development)
# WARNING: This can cause data loss in production!
DB_SYNCHRONIZE=false

# Enable SQL query logging (useful for debugging)
DB_LOGGING=false

# Server Configuration
# Port on which the API server will run
PORT=3000

# Frontend URL (for CORS configuration)
FRONTEND_URL=http://localhost:5173

# Environment
# Set to 'production' to disable Swagger and enable production optimizations
NODE_ENV=development
